<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="0dp"
    android:layout_height="match_parent"
    android:layout_weight="1"
    android:background="?attr/selectableItemBackgroundBorderless"
    android:clickable="true"
    android:focusable="true">

    <ImageView
        android:id="@+id/btnShortClick"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="center"
        android:scaleType="centerInside"
        app:tint="@color/chrome_hint" />

    <!-- Tiny indicators for secondary actions if we still want them -->
    <ImageView
        android:id="@+id/btnSwipeUp"
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:layout_gravity="top|end"
        android:layout_margin="4dp"
        android:scaleType="centerInside"
        app:tint="@color/chrome_hint"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/btnLongClick"
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:layout_gravity="bottom|end"
        android:layout_margin="4dp"
        android:scaleType="centerInside"
        app:tint="@color/chrome_hint"
        android:visibility="gone" />

    <TextView
        android:id="@+id/txtText"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:layout_gravity="center"
        android:gravity="center"
        android:textColor="@color/chrome_hint"
        android:textSize="10sp"
        android:textStyle="bold"
        android:background="@drawable/tab_count_bg"
        android:visibility="gone" />

</FrameLayout>
